<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="buttonStyle">@style/BackgroundButton</item>
        <item name="android:windowBackground">@drawable/background</item>
    </style>

    <style name="AppThemeOld" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="buttonStyle">@style/BackgroundButton</item>
    </style>

    <style name="BackgroundButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_drawable</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:padding">8dp</item>
        <item name="android:textColor">@color/text_color</item>
    </style>

</resources>
